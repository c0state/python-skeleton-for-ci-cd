dependencies:
  pre:
    - sudo ~/$CIRCLE_PROJECT_REPONAME/provisioning/install_vagrant.sh
    - sudo ~/$CIRCLE_PROJECT_REPONAME/provisioning/install_virtualbox.sh
    - cd ~/$CIRCLE_PROJECT_REPONAME && vagrant up
